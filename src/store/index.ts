import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

/*
* State management of application.
* This is a simple app and state management not needed. But if the scope
* increases and data passing required in the components then use this.
*/
export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});
